import React from 'react';
import css from './Authorization.module.scss';

const Authorization: React.FC = () => {
  return <div className={css.background}>login</div>;
};
export default Authorization;
